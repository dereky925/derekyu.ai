/**
 * About-page hero backdrop.
 * Switch `heroBackground` to try something new — `"damascus"` always stays available.
 */
export type HeroBackgroundId = "damascus";

/** Active About hero backdrop. */
export const heroBackground: HeroBackgroundId = "damascus";
